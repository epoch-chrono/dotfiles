# Environment / secrets
.env
.envrc

# chezmoi local state
.chezmoistate

# OS
.DS_Store
Thumbbs.db

# IDE
.idea/
.vscode/
.cursor/
*.swp
*~

# Python (pre-commit)
__pycache__/
*.pyc
.venv/

# mise local config
.mise.local.toml

# Ansible — collections instaladas via galaxy + arquivos locais
ansible/collections/
ansible/vars/local.yml
*.retry

# Caveman / coding-agent skills — npx skills add cria no CWD em vez de $HOME.
# Fix aplicado no role chezmoi (cd $HOME && npx ...), mas se reaparecer,
# ignorar pra evitar poluir o repo.
.agents/
skills-lock.json
