# Pacotes npm globais instalados em cada `mise install node@X`.
# Lista enxuta — só items que PRECISAM estar globais ao lado do
# commitizen (plugins do cz são procurados em $NPM_PREFIX/node_modules).
#
# Lista completa do Mac antigo tinha 15 itens. Decidir caso a caso:
#
#   - Aqui (.default-npm-packages):  pacotes que precisam estar globais
#                                    pra outro tool detectar (plugins
#                                    commitizen, etc).
#   - [tools] do mise config:        CLIs Node genéricos via npm:pacote
#                                    (mise usa aube automaticamente
#                                    quando disponível, ~9x mais rápido).
#
# Sugestões pra avaliar (estavam no Mac antigo, removidos daqui):
#
#   Candidatos a npm: no [tools]:
#     commitizen, dockly, create-slidev, localtunnel, aws-cost-cli,
#     sql-formatter, artillery, @clevercli/cli, git-tip
#
#   Não migrados (per-project ou deprecated):
#     jest, ts-jest        → per-project (em package.json devDeps)
#     asciicast2gif        → deprecated (asciinema-agg é a alternativa)
#     devmoji              → redundante com cz-emoji

# Commitizen plugins — descoberta automática pelo commitizen via
# $NPM_PREFIX/lib/node_modules, então precisam estar no global Node.
cz-emoji
cz-jira-smart-emoji
