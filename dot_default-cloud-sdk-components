# Componentes do gcloud-sdk auto-instalados após `mise install gcloud`.
# Lista lida pelo plugin/backend que provê o gcloud-sdk no mise (ou pelo
# gcloud install diretamente). Mantido aqui em $HOME por convenção do
# gcloud-sdk — não tem alternativa via backend mise (são plugins internos
# do próprio gcloud).
#
# Para ver o que cada componente faz:
#   gcloud components list

gke-gcloud-auth-plugin
beta
alpha
